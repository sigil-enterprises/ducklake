{
  "description": "Run DuckLake tests using MinIO.",
  "on_init": "CREATE SECRET (TYPE S3, KEY_ID 'admin', SECRET 'password', ENDPOINT '127.0.0.1:9000', URL_STYLE 'path', USE_SSL 0);",
  "autoloading": "all",
  "test_env": [{
      "env_name":  "DATA_PATH",
      "env_value": "s3:/"
   }],
  "statically_loaded_extensions": [
    "core_functions",
    "parquet",
    "ducklake",
    "icu",
    "httpfs"
  ]
}